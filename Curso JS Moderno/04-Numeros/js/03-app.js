let resultado;

//PI
resultado = Math.PI;

//Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.4);

//Redondear para arriba
resultado = Math.ceil(2.1); //Siempre que se mayor de 2.1 redondea hacia arriba

//Redondear hacia abajo
resultado = Math.floor(2.9);

//Raiz cuadrada
resultado = Math.sqrt(15151);

//Absoluto
resultado = Math.abs(-500);

//Potencia
resultado = Math.pow(2, 4);

//Minimo
resultado = Math.min(3, 1);

//Máximo
resultado = Math.max(4, 10, 2);

//Aleatorio
resultado = Math.random();

//Aleatorio dentro de un rango...
resultado = Math.ceil(Math.random() * 30); //También puede ser Math.floor

console.log(resultado);
