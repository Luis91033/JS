//QuerySelectorAll
//Retorna todos los elementos que cumplan con la condición
const card = document.querySelectorAll(".card");
console.log(card);

const formularios = document.querySelectorAll("#formulario");
console.log(formulario);

//Si un elemento no existe
const noExiste = document.querySelectorAll(".no-existe"); //Regresa un arreglo vacio
console.log(noExiste);
