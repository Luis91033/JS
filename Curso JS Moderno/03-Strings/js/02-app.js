//Métodos

const producto = "Monitor de 20 pulgadas";

// Conocer la cantidad de letras de la cadena de texto
console.log(producto.length);

// Sirve para saber si en la variable está la palabra y te regresa la posición
console.log(producto.indexOf("Monitor"));

// Sirve para saber si en la variable está la palabra y te regresa true o false
console.log(producto.includes("Monitor"));
